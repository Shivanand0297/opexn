import { HeroScrollContainer, HeroSlider } from "@/components/home";

export default function Home() {
  return (
    <section>
      <HeroSlider />
      <HeroScrollContainer />
    </section>
  );
}
